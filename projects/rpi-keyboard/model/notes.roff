.SH
Hello

.PP
Distance between line and point can be obtained using following
equation:
.EQ
d(({ P sub 1 }, alpha ), ({ x sub 0 }, { y sub 0 })) = {
|~ {( x sub 0 - x sub 1 ) + tg sup 2 ( y sub 1 - y sub 0 )} ~|
} over {
sqrt { 1 + tg sup 2 alpha}
}
.EN

.PP
