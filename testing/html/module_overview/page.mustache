<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="refresh" content="2">
    </head>
    <style>
      aside {
        height: 100%;
        width: 160px;
        position: fixed;
        z-index: 1;
        top: 0;
        left: 0;
        background-color: #111;
        overflow-x: hidden;
        padding-top: 20px;
      }

      aside a {
        padding: 6px 8px 6px 16px;
        text-decoration: none;
        font-size: 16px;
        color: #818181;
        display: block;
      }

      aside a:hover {
        color: #f1f1f1;
      }

      main {
        margin-left: 160px;
        padding: 0px 10px;
      }
    </style>
    <body>
        <aside>
        {{#packages}}
            <a class="module-link" href="#">{{name}}</a>
        {{/packages}}
        </aside>

        <main>
        {{#packages}}
            <h1>{{name}}</h1>
            {{docText.docBrief}}

            {{#modules}}
                <h2>{{name}}</h2>
                {{docText.docBrief}}
                <table>
                    <tr><th>Category</th><th>Functions</th></tr>
                    <!-- There is no 'real' data for `.categorires` field - it is created
                         on-demand by iterating through all subnodes.
                         "categories" is a regular key-value pair, but `ProcKinds` is
                         generated by value provider which iterates over subnodes of
                         current module.
                    -->
                    {{#categories.ProcKinds}}
                    <tr><th>{{name}}</th><th>
                        {{#items}}<a href="{{link}}">{{name}}</a>{{/items}}
                    </th></tr>
                    {{/categories.ProcKinds}}
                </table>
                {{#types}}
                    <h3>{{name}}</h3>
                {{/types}}
            {{/modules}}
        {{/packages}}
        </main>
    </body>
</html>
