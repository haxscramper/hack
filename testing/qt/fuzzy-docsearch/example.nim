proc helloWorld(arg: cfloat): cfloat =
  discard

proc getSign(a1: string, a2: string, a3: int): int =
  discard

proc name(o: Object): string =
  discard

proc secondName(o: Object): string =
  discard
