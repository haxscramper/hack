:- initialization(main).

is_really_fucking_list(List) :-
    is_list(List).

test(List) :-
    writeln(List), is_really_fucking_list(List) ; writeln("Not a list").

main :-
    test([12]),
    test([]),
    halt.
