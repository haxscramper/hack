:- initialization(main).

get_last([Head], Head).
get_last([_|Tail], Elem) :- get_last(Tail, Elem).

get_last_print(List) :- get_last(List, Out), writeln(Out).

interactive :-
    nl
.

test :-
    get_last_print([1,2,3]),
    get_last_print([1])
.

main :- test, halt.
