[project]
name = "header-source-reorder-v2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "haxscramper", email = "haxscramper@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "beartype>=0.22.9",
    "loguru>=0.7.3",
    "tree-sitter==0.25.2",
    "tree-sitter-cpp>=0.23.4",
]

[project.scripts]
header-source-reorder-v2 = "header_source_reorder_v2:main"

[build-system]
requires = ["uv_build>=0.9.27,<0.10.0"]
build-backend = "uv_build"
